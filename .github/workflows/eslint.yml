name: eslint

on:
  push:
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: ESLint Action
      uses: stefanoeb/eslint-action@1.0.2
